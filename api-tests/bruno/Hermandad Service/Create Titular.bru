meta {
  name: Create Titular
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/hermandades/{{hermandad_id}}/titulares
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Jesús Nazareno",
    "description": "Paso de misterio"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setVar("titular_id", data.id);
}
