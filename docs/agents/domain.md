# Domain Docs

Single-context repo. One `AGENTS.md` + `docs/` at the root.

## Before exploring, read these

- **`AGENTS.md`** at the repo root — project invariants, development workflow, common mistakes
- **`docs/functional-map.md`** — complete topology, endpoints, DB schemas, test inventory (960 lines)
- **`docs/architecture.md`** — hexagonal + DDD design decisions
- **`docs/roadmap.md`** — development phases and portfolio milestones

## Flag ADR conflicts

If your output contradicts an existing architecture decision, surface it explicitly rather than silently overriding.
