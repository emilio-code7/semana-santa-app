{
  "openapi": "3.0.1",
  "info": {
    "title": "Semana Santa API",
    "version": "v0",
    "description": "Semana Santa management API — procesion service includes Pasos, Route Sections, and plan finalization."
  },
  "servers": [
    {
      "url": "http://localhost:8080",
      "description": "API Gateway (development)"
    }
  ],
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "paths": {
    "/api/hermandades": {
      "post": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "Create a new hermandad",
        "operationId": "createHermandad",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateHermandadRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "400": {
            "description": "Invalid input (validation error)",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized — missing or invalid JWT"
          },
          "403": {
            "description": "Forbidden — insufficient privileges"
          },
          "409": {
            "description": "Hermandad with this name already exists",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          },
          "201": {
            "description": "Hermandad created",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/hermandades/{hermandadId}/members": {
      "get": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "List members of a hermandad",
        "operationId": "getHermandadMembers",
        "parameters": [
          {
            "name": "hermandadId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "List of members",
            "content": {
              "*/*": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/HermandadMember"
                  }
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized — missing or invalid JWT"
          },
          "403": {
            "description": "Forbidden — insufficient privileges"
          },
          "404": {
            "description": "Hermandad not found",
            "content": {
              "*/*": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/HermandadMember"
                  }
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "Add a member to a hermandad",
        "operationId": "createHermandadMember",
        "parameters": [
          {
            "name": "hermandadId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddMemberRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Member added",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized — missing or invalid JWT"
          },
          "403": {
            "description": "Forbidden — insufficient privileges"
          },
          "404": {
            "description": "Hermandad not found",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          },
          "400": {
            "description": "Invalid input (validation error or illegal argument)",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          }
        }
      }
    },
    "/api/hermandades/{hermandadId}/members/{userId}/role": {
      "patch": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "Change a member's role",
        "operationId": "changeHermandadMemberRole",
        "parameters": [
          {
            "name": "hermandadId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "userId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChangeRoleRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "400": {
            "description": "Invalid input (validation error)",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized — missing or invalid JWT"
          },
          "403": {
            "description": "Forbidden — insufficient privileges"
          },
          "404": {
            "description": "Member not found in this hermandad",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          },
          "200": {
            "description": "Role updated",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadMember"
                }
              }
            }
          }
        }
      }
    },
    "/api/hermandades/{hermandadId}": {
      "get": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "Get a hermandad by ID",
        "operationId": "getHermandad",
        "parameters": [
          {
            "name": "hermandadId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "400": {
            "description": "Invalid ID format",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          },
          "200": {
            "description": "Hermandad found",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          },
          "404": {
            "description": "Hermandad not found",
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/HermandadResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/procesiones": {
      "post": {
        "tags": ["procesion-controller"],
        "summary": "Create a new procesion",
        "operationId": "createProcesion",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/CreateProcesionRequest" }
            }
          },
          "required": true
        },
        "responses": {
          "201": { "description": "Procesion created", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ProcesionResponse" } } } },
          "400": { "description": "Invalid input (validation error)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" }
        }
      },
      "get": {
        "tags": ["procesion-controller"],
        "summary": "List procesiones by hermandad (paginated)",
        "operationId": "listByHermandad",
        "parameters": [
          { "name": "hermandadId", "in": "query", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "page", "in": "query", "schema": { "type": "integer", "default": 0 } },
          { "name": "size", "in": "query", "schema": { "type": "integer", "default": 20 } }
        ],
        "responses": {
          "200": {
            "description": "Paginated list of procesiones",
            "content": {
              "*/*": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "content": { "type": "array", "items": { "$ref": "#/components/schemas/ProcesionResponse" } },
                    "totalElements": { "type": "integer" },
                    "totalPages": { "type": "integer" },
                    "number": { "type": "integer" },
                    "size": { "type": "integer" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/procesiones/{id}": {
      "get": {
        "tags": ["procesion-controller"],
        "summary": "Get a procesion by ID",
        "operationId": "getProcesion",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Procesion found", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ProcesionResponse" } } } },
          "404": { "description": "Procesion not found" }
        }
      },
      "delete": {
        "tags": ["procesion-controller"],
        "summary": "Delete a procesion",
        "operationId": "deleteProcesion",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "204": { "description": "Procesion deleted" },
          "404": { "description": "Procesion not found" }
        }
      }
    },
    "/api/procesiones/{id}/status": {
      "patch": {
        "tags": ["procesion-controller"],
        "summary": "Change procesion status",
        "operationId": "changeStatus",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/StatusChangeRequest" }
            }
          },
          "required": true
        },
        "responses": {
          "200": { "description": "Status changed", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ProcesionResponse" } } } },
          "400": { "description": "Invalid status transition" },
          "404": { "description": "Procesion not found" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/procesiones/{procesionId}/pasos": {
      "get": {
        "tags": ["paso-controller"],
        "summary": "Get ordered list of pasos for a procesion",
        "operationId": "getPasos",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Ordered list of pasos", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ReplacePasosResponse" } } } },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant access" },
          "404": { "description": "Procesion not found" }
        }
      },
      "put": {
        "tags": ["paso-controller"],
        "summary": "Atomically replace the ordered list of pasos",
        "operationId": "replacePasos",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ReplacePasosRequest" } } } },
        "responses": {
          "200": { "description": "Pasos replaced", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ReplacePasosResponse" } } } },
          "400": { "description": "Invalid input (validation error or duplicate position)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant or titular from another hermandad" },
          "404": { "description": "Procesion not found" },
          "409": { "description": "Conflict — plan already finalized, pasos are immutable" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/procesiones/{procesionId}/route": {
      "get": {
        "tags": ["procesion-plan-controller"],
        "summary": "Get ordered route sections for a procesion",
        "operationId": "getRouteSections",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Ordered list of route sections", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/RouteSectionsResponse" } } } },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant access" },
          "404": { "description": "Procesion not found" }
        }
      },
      "put": {
        "tags": ["procesion-plan-controller"],
        "summary": "Atomically replace draft route sections for a procesion",
        "operationId": "replaceRouteSections",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ReplaceRouteSectionsRequest" } } } },
        "responses": {
          "200": { "description": "Route sections replaced", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/RouteSectionsResponse" } } } },
          "400": { "description": "Invalid input (validation error or duplicate position)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant access" },
          "404": { "description": "Procesion not found" },
          "409": { "description": "Conflict — plan already finalized, route is immutable" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/procesiones/{procesionId}/plan/finalize": {
      "post": {
        "tags": ["procesion-plan-controller"],
        "summary": "Finalize the procesion plan, making pasos and route immutable",
        "operationId": "finalizePlan",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Plan finalized", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/FinalizePlanResponse" } } } },
          "400": { "description": "Bad request — no pasos or no route sections defined" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant access" },
          "404": { "description": "Procesion not found" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/titulares": {
      "post": {
        "tags": ["titular-controller"],
        "summary": "Create a titular for a hermandad",
        "operationId": "createTitular",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/CreateTitularRequest" } } } },
        "responses": {
          "201": { "description": "Titular created", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/TitularResponse" } } } },
          "400": { "description": "Invalid input (validation error)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — not admin or capataz of this hermandad" }
        }
      },
      "get": {
        "tags": ["titular-controller"],
        "summary": "List titulares of a hermandad",
        "operationId": "listTitulares",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "List of titulares", "content": { "*/*": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/TitularResponse" } } } } },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — not a member of this hermandad" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/titulares/{id}": {
      "get": {
        "tags": ["titular-controller"],
        "summary": "Get a titular by ID",
        "operationId": "getTitular",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Titular found", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/TitularResponse" } } } },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — not a member of this hermandad" },
          "404": { "description": "Titular not found" }
        }
      },
      "put": {
        "tags": ["titular-controller"],
        "summary": "Update a titular",
        "operationId": "updateTitular",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/UpdateTitularRequest" } } } },
        "responses": {
          "200": { "description": "Titular updated", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/TitularResponse" } } } },
          "400": { "description": "Invalid input (validation error)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — not admin or capataz of this hermandad" },
          "404": { "description": "Titular not found" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/members/{userId}": {
      "delete": {
        "tags": [
          "hermandad-controller"
        ],
        "summary": "Delete a member from hermandad",
        "operationId": "deleteHermandadMember",
        "parameters": [
          {
            "name": "hermandadId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "userId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Member deleted"
          },
          "401": {
            "description": "Unauthorized — missing or invalid JWT"
          },
          "403": {
            "description": "Forbidden — insufficient privileges"
          },
          "404": {
            "description": "Member not found in this hermandad"
          }
        }
      }
    },
    "/api/marchas": {
      "post": {
        "tags": ["marcha-controller"],
        "summary": "Create a new marcha",
        "operationId": "createMarcha",
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/MarchaRequest" } } } },
        "responses": {
          "201": { "description": "Marcha created", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/MarchaResponse" } } } },
          "400": { "description": "Invalid input (validation error)" }
        }
      },
      "get": {
        "tags": ["marcha-controller"],
        "summary": "List all marchas",
        "operationId": "listMarchas",
        "responses": {
          "200": { "description": "List of marchas", "content": { "*/*": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/MarchaResponse" } } } } }
        }
      }
    },
    "/api/marchas/search": {
      "get": {
        "tags": ["marcha-controller"],
        "summary": "Search marchas by title or composer",
        "operationId": "searchMarchas",
        "parameters": [ { "name": "q", "in": "query", "required": true, "schema": { "type": "string" } } ],
        "responses": {
          "200": { "description": "Matching marchas (empty array if none)", "content": { "*/*": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/MarchaResponse" } } } } },
          "400": { "description": "Missing required query parameter q" },
          "401": { "description": "Unauthorized — missing or invalid JWT" }
        }
      }
    },
    "/api/marchas/{id}": {
      "get": {
        "tags": ["marcha-controller"],
        "summary": "Get a marcha by ID",
        "operationId": "getMarcha",
        "parameters": [ { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } } ],
        "responses": {
          "200": { "description": "Marcha found", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/MarchaResponse" } } } },
          "404": { "description": "Marcha not found" }
        }
      },
      "put": {
        "tags": ["marcha-controller"],
        "summary": "Update a marcha",
        "operationId": "updateMarcha",
        "parameters": [ { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } } ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/MarchaRequest" } } } },
        "responses": {
          "200": { "description": "Marcha updated", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/MarchaResponse" } } } },
          "400": { "description": "Invalid input (validation error)" },
          "404": { "description": "Marcha not found" }
        }
      },
      "delete": {
        "tags": ["marcha-controller"],
        "summary": "Delete a marcha",
        "operationId": "deleteMarcha",
        "parameters": [ { "name": "id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } } ],
        "responses": {
          "204": { "description": "Marcha deleted" },
          "404": { "description": "Marcha not found" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/procesiones/{procesionId}/pasos": {
      "get": {
        "tags": ["paso-controller"],
        "summary": "List pasos for a procesion",
        "operationId": "getPasos",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Ordered list of pasos", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ReplacePasosResponse" } } } },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant access" },
          "404": { "description": "Procesion not found" }
        }
      },
      "put": {
        "tags": ["paso-controller"],
        "summary": "Atomically replace the ordered list of pasos for a procesion",
        "operationId": "replacePasos",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ReplacePasosRequest" } } } },
        "responses": {
          "200": { "description": "Pasos replaced", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/ReplacePasosResponse" } } } },
          "400": { "description": "Invalid input (validation error or duplicate position)" },
          "401": { "description": "Unauthorized — missing or invalid JWT" },
          "403": { "description": "Forbidden — cross-tenant or titular from another hermandad" },
          "404": { "description": "Procesion not found" }
        }
      }
    },
    "/api/hermandades/{hermandadId}/procesiones/{procesionId}/cruceta": {
      "get": {
        "tags": ["cruceta-controller"],
        "summary": "Get cruceta for a procesion",
        "operationId": "getCruceta",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "responses": {
          "200": { "description": "Cruceta found", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/CrucetaResponse" } } } },
          "404": { "description": "Cruceta not found" }
        }
      },
      "put": {
        "tags": ["cruceta-controller"],
        "summary": "Define or replace cruceta for a procesion",
        "operationId": "defineCruceta",
        "parameters": [
          { "name": "hermandadId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } },
          { "name": "procesionId", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" } }
        ],
        "requestBody": { "required": true, "content": { "application/json": { "schema": { "$ref": "#/components/schemas/CrucetaRequest" } } } },
        "responses": {
          "200": { "description": "Cruceta defined", "content": { "*/*": { "schema": { "$ref": "#/components/schemas/CrucetaResponse" } } } },
          "400": { "description": "Invalid input (validation error)" },
          "403": { "description": "Forbidden — not admin of this hermandad" },
          "404": { "description": "Procesion not known yet (consumer may still be processing the ProcesionCreatedEvent)" }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "CreateHermandadRequest": {
        "required": [
          "city",
          "name",
          "foundedYear"
        ],
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "city": {
            "type": "string"
          },
          "foundedYear": {
            "minimum": 1,
            "type": "integer",
            "format": "int32"
          },
          "description": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "HermandadResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "city": {
            "type": "string"
          },
          "foundedYear": {
            "type": "integer",
            "format": "int32"
          },
          "description": {
            "type": "string",
            "nullable": true
          },
          "keycloakGroupId": {
            "type": "string",
            "nullable": true
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "AddMemberRequest": {
        "required": [
          "role",
          "userId"
        ],
        "type": "object",
        "properties": {
          "userId": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "CAPATAZ",
              "HERMANDAD_ADMIN",
              "BAND_DIRECTOR",
              "MUSICIAN"
            ]
          }
        }
      },
      "HermandadMember": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "hermandadId": {
            "type": "string",
            "format": "uuid"
          },
          "userId": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "CAPATAZ",
              "HERMANDAD_ADMIN",
              "BAND_DIRECTOR",
              "MUSICIAN"
            ]
          },
          "joinedAt": {
            "type": "string",
            "format": "date-time"
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "CreateProcesionRequest": {
        "required": ["hermandadId", "date", "time"],
        "type": "object",
        "properties": {
          "hermandadId": { "type": "string", "format": "uuid" },
          "date": { "type": "string", "format": "date" },
          "time": { "type": "string", "format": "time" }
        }
      },
      "ProcesionResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "hermandadId": { "type": "string", "format": "uuid" },
          "date": { "type": "string", "format": "date" },
          "time": { "type": "string", "format": "time" },
          "status": { "type": "string", "enum": ["PLANNED", "IN_PROGRESS", "COMPLETED", "CANCELLED"] },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "ProcesionResponseFinalized": {
        "allOf": [
          { "$ref": "#/components/schemas/ProcesionResponse" },
          {
            "type": "object",
            "properties": {
              "planFinalizedAt": { "type": "string", "format": "date-time", "nullable": true }
            }
          }
        ]
      },
      "ReplacePasosRequest": {
        "required": ["pasos"],
        "type": "object",
        "properties": {
          "pasos": { "type": "array", "items": { "$ref": "#/components/schemas/PasoItemRequest" }, "minItems": 1 }
        }
      },
      "PasoItemRequest": {
        "required": ["position", "titularId"],
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid", "nullable": true },
          "position": { "type": "integer", "format": "int32", "minimum": 0 },
          "titularId": { "type": "string", "format": "uuid" },
          "notes": { "type": "string", "nullable": true }
        }
      },
      "ReplacePasosResponse": {
        "type": "object",
        "properties": {
          "pasos": { "type": "array", "items": { "$ref": "#/components/schemas/PasoItemResponse" } }
        }
      },
      "PasoItemResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "procesionId": { "type": "string", "format": "uuid" },
          "position": { "type": "integer", "format": "int32" },
          "titularId": { "type": "string", "format": "uuid" },
          "notes": { "type": "string", "nullable": true },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "ReplaceRouteSectionsRequest": {
        "required": ["sections"],
        "type": "object",
        "properties": {
          "sections": { "type": "array", "items": { "$ref": "#/components/schemas/RouteSectionRequest" }, "minItems": 0 }
        }
      },
      "RouteSectionRequest": {
        "required": ["name", "position"],
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid", "nullable": true },
          "name": { "type": "string" },
          "position": { "type": "integer", "format": "int32", "minimum": 0 },
          "notes": { "type": "string", "nullable": true }
        }
      },
      "RouteSectionsResponse": {
        "type": "object",
        "properties": {
          "sections": { "type": "array", "items": { "$ref": "#/components/schemas/RouteSectionResponse" } }
        }
      },
      "RouteSectionResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "procesionId": { "type": "string", "format": "uuid" },
          "name": { "type": "string" },
          "position": { "type": "integer", "format": "int32" },
          "notes": { "type": "string", "nullable": true },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "FinalizePlanResponse": {
        "allOf": [
          { "$ref": "#/components/schemas/ProcesionResponse" },
          {
            "type": "object",
            "properties": {
              "planFinalizedAt": { "type": "string", "format": "date-time" },
              "pasoCount": { "type": "integer", "format": "int32" },
              "routeSectionCount": { "type": "integer", "format": "int32" }
            }
          }
        ]
      },
      "StatusChangeRequest": {
        "required": ["newStatus"],
        "type": "object",
        "properties": {
          "newStatus": {
            "type": "string",
            "enum": ["PLANNED", "IN_PROGRESS", "COMPLETED", "CANCELLED"]
          }
        }
      },
      "ChangeRoleRequest": {
        "required": [
          "role"
        ],
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "enum": [
              "CAPATAZ",
              "HERMANDAD_ADMIN",
              "BAND_DIRECTOR",
              "MUSICIAN"
            ]
          }
        }
      },
      "MarchaRequest": {
        "required": ["title", "composer", "bandType", "durationSeconds"],
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "composer": { "type": "string" },
          "bandType": { "type": "string", "enum": ["BANDA_PALIO", "AGRUPACION_MUSICAL", "BANDA_CORNETAS"] },
          "durationSeconds": { "type": "integer", "format": "int32", "minimum": 1 },
          "compositionYear": { "type": "integer", "format": "int32", "nullable": true },
          "youtubeUrl": { "type": "string", "nullable": true }
        }
      },
      "MarchaResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "title": { "type": "string" },
          "composer": { "type": "string" },
          "bandType": { "type": "string", "enum": ["BANDA_PALIO", "AGRUPACION_MUSICAL", "BANDA_CORNETAS"] },
          "durationSeconds": { "type": "integer", "format": "int32" },
          "compositionYear": { "type": "integer", "format": "int32", "nullable": true },
          "youtubeUrl": { "type": "string", "nullable": true },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "CreateTitularRequest": {
        "required": ["name"],
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "description": { "type": "string", "nullable": true }
        }
      },
      "UpdateTitularRequest": {
        "required": ["name"],
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "description": { "type": "string", "nullable": true }
        }
      },
      "TitularResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "hermandadId": { "type": "string", "format": "uuid" },
          "name": { "type": "string" },
          "description": { "type": "string", "nullable": true },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "CrucetaRequest": {
        "required": ["items"],
        "type": "object",
        "properties": {
          "items": { "type": "array", "items": { "$ref": "#/components/schemas/CrucetaItemRequest" }, "minItems": 1 }
        }
      },
      "CrucetaItemRequest": {
        "required": ["marchaId", "orderIndex"],
        "type": "object",
        "properties": {
          "marchaId": { "type": "string", "format": "uuid" },
          "orderIndex": { "type": "integer", "format": "int32", "minimum": 0 },
          "notes": { "type": "string", "nullable": true }
        }
      },
      "CrucetaResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "procesionId": { "type": "string", "format": "uuid" },
          "items": { "type": "array", "items": { "$ref": "#/components/schemas/CrucetaItemResponse" } },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "CrucetaItemResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "marchaId": { "type": "string", "format": "uuid" },
          "orderIndex": { "type": "integer", "format": "int32" },
          "notes": { "type": "string", "nullable": true }
        }
      },
      "ReplacePasosRequest": {
        "required": ["pasos"],
        "type": "object",
        "properties": {
          "pasos": { "type": "array", "items": { "$ref": "#/components/schemas/PasoItemRequest" }, "minItems": 1 }
        }
      },
      "PasoItemRequest": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid", "nullable": true, "description": "Omit for new pasos; include existing IDs for stability" },
          "position": { "type": "integer", "format": "int32", "minimum": 0 },
          "titularId": { "type": "string", "format": "uuid" },
          "notes": { "type": "string", "nullable": true }
        }
      },
      "ReplacePasosResponse": {
        "type": "object",
        "properties": {
          "pasos": { "type": "array", "items": { "$ref": "#/components/schemas/PasoItemResponse" } }
        }
      },
      "PasoItemResponse": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "procesionId": { "type": "string", "format": "uuid" },
          "position": { "type": "integer", "format": "int32", "minimum": 0 },
          "titularId": { "type": "string", "format": "uuid" },
          "notes": { "type": "string", "nullable": true },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "description": "Paste your Keycloak access token here.\n\nGet one via:\n  curl -s -X POST http://localhost:8180/realms/semana-santa/protocol/openid-connect/token \\\n    -d \"grant_type=password&client_id=semana-santa-client&client_secret=secret&username=qa-admin-user&password=test\" \\\n    | jq -r '.access_token'",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  }
}
